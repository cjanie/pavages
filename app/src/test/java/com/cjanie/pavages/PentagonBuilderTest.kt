package com.cjanie.pavages

import com.cjanie.pavages.logic.Cercle
import com.cjanie.pavages.logic.PentagonBuilder
import com.cjanie.pavages.logic.Point
import org.junit.Assert.assertEquals
import org.junit.Assert.assertNotNull
import org.junit.Test

class PentagonBuilderTest {
/*
    @Test
    fun `K on the vertical axis, in the middle between O and A'`() {
        val K = PentagonBuilder(Cercle(Point(x = 0.0, y = 0.0), 2.0)).K
        assertEquals(0.0, K.x, 0.0)
        assertEquals(-1.0, K.y, 0.0)
    }

    @Test
    fun `has golden triangle`() {
        val goldenTriangle = PentagonBuilder(Cercle(Point(x = 0.0, y = 0.0), 6.0)).build().goldenTriangle
        assertNotNull(goldenTriangle)
    }

    @Test
    fun `has two golden gnomons`() {
        val goldenGnomons = PentagonBuilder(Cercle(Point(x = 0.0, y = 0.0), 6.0)).build().goldenGnomons
        assertEquals(2, goldenGnomons.size)
    }

 */
}